import React, { Component } from 'react'

class Offline extends Component{
  render() {
    return(
      <div>
        Sorry, it appears that the app is offline.
      </div>
    )
  }
}
export default Offline;
